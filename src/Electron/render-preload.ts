//import epub from '../../externals/builds/epub';

document.addEventListener('DOMContentLoaded', () => {
  console.log('preloading 2 the sequel');

  console.log("in preloader : " + document.getElementById("render-area"));
});